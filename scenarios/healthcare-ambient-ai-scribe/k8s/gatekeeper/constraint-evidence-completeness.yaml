# ================================================================
# GenAIOps PoC — Gatekeeper Constraint: Evidence Completeness
# ================================================================
# G-OPS-05: Evidence-Completeness und Audit-Trail-Integrität
#
# Ensures every Deployment in namespace "genaiops" has:
#   1. genaiops.io/evidence-store-connected: "true"
#   2. genaiops.io/hash-chain-enabled: "true"
#
# Trigger: kubectl apply → Gatekeeper Admission Controller
# Policy:  policy_evidence_completeness + policy_hash_chain_integrity
# ================================================================
apiVersion: templates.gatekeeper.sh/v1
kind: ConstraintTemplate
metadata:
  name: genaiopsevidencecompleteness
  labels:
    app.kubernetes.io/part-of: genaiops-compliance-gates
    app.kubernetes.io/component: gatekeeper
spec:
  crd:
    spec:
      names:
        kind: GenaiopsEvidenceCompleteness
      validation:
        openAPIV3Schema:
          type: object
          properties:
            requiredAnnotations:
              type: array
              items:
                type: object
                properties:
                  key:
                    type: string
                  value:
                    type: string
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
        package genaiopsevidencecompleteness

        # Rule 1: Annotations block missing entirely
        violation[{"msg": msg}] {
          input.review.object.kind == "Deployment"
          not input.review.object.spec.template.metadata.annotations
          required := input.parameters.requiredAnnotations[_]
          msg := sprintf("G-OPS-05 FAIL: Pod template has no annotations. Required: '%v'='%v'", [required.key, required.value])
        }

        # Rule 2: Annotation present but wrong value
        violation[{"msg": msg}] {
          input.review.object.kind == "Deployment"
          annotations := input.review.object.spec.template.metadata.annotations
          required := input.parameters.requiredAnnotations[_]
          val := annotations[required.key]
          val != required.value
          msg := sprintf("G-OPS-05 FAIL: Pod annotation '%v' must be '%v', got '%v'", [required.key, required.value, val])
        }

        # Rule 3: Annotation key missing
        violation[{"msg": msg}] {
          input.review.object.kind == "Deployment"
          annotations := input.review.object.spec.template.metadata.annotations
          required := input.parameters.requiredAnnotations[_]
          not annotations[required.key]
          msg := sprintf("G-OPS-05 FAIL: Pod annotation '%v' is missing", [required.key])
        }
---
apiVersion: constraints.gatekeeper.sh/v1beta1
kind: GenaiopsEvidenceCompleteness
metadata:
  name: require-evidence-annotations
  # NOTE: Constraints are cluster-scoped; namespace filtering via spec.match
  labels:
    app.kubernetes.io/part-of: genaiops-compliance-gates
    app.kubernetes.io/component: gatekeeper
spec:
  enforcementAction: deny
  match:
    kinds:
      - apiGroups: ["apps"]
        kinds: ["Deployment"]
    namespaces: ["genaiops"]
  parameters:
    requiredAnnotations:
      - key: "genaiops.io/evidence-store-connected"
        value: "true"
      - key: "genaiops.io/hash-chain-enabled"
        value: "true"
